<section class="section">
  <div class="container">
    <div class="columns transitions-enabled centered clearfix masonry">
      <!--
    	<% if user_signed_in? %>
    		<%= render 'profile' %>
    	<% else %>
       <%= render 'trends' %>
      <% end %>
-->
       <%= render 'feed' %>
       <!--
       <%= render 'who-to-follow' %>
     -->
    </div>
  </div>
</section>
